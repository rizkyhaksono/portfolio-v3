---
title: "Security & RBAC"
description: "ServiceAccounts, Roles, RoleBindings, Pod Security Standards, and secrets"
course: "kubernetes-fundamentals"
category: "devops"
difficulty: "advanced"
level: "advanced"
duration: "26 min"
order: 8
illustration: "https://images.unsplash.com/photo-1667372393119-3d4c48d91fcb?q=80&w=2940&auto=format&fit=crop"
---

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion"

## Kubernetes Security

<LearningObjectives objectives={["Apply least-privilege RBAC", "Mount secrets safely", "Enforce Pod Security Standards", "Rotate credentials regularly"]} />

```yaml
kind: Role
metadata:
  name: pod-reader
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
---
kind: RoleBinding
metadata:
  name: read-pods
subjects:
  - kind: ServiceAccount
    name: app-sa
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
```

<Warning>Never commit plain-text secrets to git — use Sealed Secrets or external secret operators.</Warning>

---

## Next Steps

<NextLesson title="GitOps & CI/CD" description="ArgoCD deployments." href="/roadmap/kubernetes-fundamentals-advanced-gitops-cicd" />

<Resources resources={[{"title":"RBAC","href":"https://kubernetes.io/docs/reference/access-authn-authz/rbac/"}]} />
